import { reactive } from "vue";

export const entries = reactive([])
export const settings = reactive({
    userName: 'Marcus',
    targetBG: 5.5,
    goodDelta: 0.5,
    okDelta: 1.2,
    defaultDuration: 120,
    defaultZone: 'morning'
})